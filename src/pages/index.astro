---
import BlogLayout from '../layouts/BlogLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const latestPost = posts[0];
const otherPosts = posts.slice(1);
---

<BlogLayout title="My Blog - Home" description="Welcome to my blog about web development">
	<!-- Hero Section -->
	<section class="text-center py-16">
		<h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
			Welcome to My Blog
		</h1>
		<p class="text-xl text-gray-600 max-w-2xl mx-auto">
			Sharing my thoughts on web development, coding, and technology.
		</p>
	</section>

	<!-- Latest Post -->
	{latestPost && (
		<section class="mb-12">
			<h2 class="text-2xl font-semibold text-gray-900 mb-6">Latest Post</h2>
			<article class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-md transition">
				<a href={`/blog/${latestPost.slug}`} class="block">
					<div class="aspect-video bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center">
						<span class="text-white text-6xl opacity-50">📝</span>
					</div>
					<div class="p-6">
						<div class="flex items-center gap-2 text-sm text-gray-500 mb-2">
							<time datetime={latestPost.data.pubDate.toISOString()}>
								{latestPost.data.pubDate.toLocaleDateString('en-US', {
									year: 'numeric',
									month: 'long',
									day: 'numeric',
								})}
							</time>
						</div>
						<h3 class="text-2xl font-bold text-gray-900 mb-2 hover:text-indigo-600 transition">
							{latestPost.data.title}
						</h3>
						<p class="text-gray-600">{latestPost.data.description}</p>
					</div>
				</a>
			</article>
		</section>
	)}

	<!-- Other Posts -->
	{otherPosts.length > 0 && (
		<section>
			<h2 class="text-2xl font-semibold text-gray-900 mb-6">Recent Posts</h2>
			<div class="grid md:grid-cols-2 gap-6">
				{otherPosts.map((post) => (
					<article class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-md transition">
						<a href={`/blog/${post.id}`} class="block p-6">
							<div class="flex items-center gap-2 text-sm text-gray-500 mb-2">
								<time datetime={post.data.pubDate.toISOString()}>
									{post.data.pubDate.toLocaleDateString('en-US', {
										year: 'numeric',
										month: 'long',
										day: 'numeric',
									})}
								</time>
							</div>
							<h3 class="text-xl font-bold text-gray-900 mb-2 hover:text-indigo-600 transition">
								{post.data.title}
							</h3>
							<p class="text-gray-600">{post.data.description}</p>
						</a>
					</article>
				))}
			</div>
		</section>
	)}

	<!-- No Posts -->
	{posts.length === 0 && (
		<p class="text-center text-gray-600 py-16">No posts yet. Check back soon!</p>
	)}
</BlogLayout>